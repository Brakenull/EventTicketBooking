spring:
  application:
    name: booking
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: ${DB_USERNAME:ten_dang_nhap_cua_database}
    password: ${DB_PASSWORD:mat_khau_cua_database}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        highlight_sql: true
  data:
    redis:
      host: localhost
      port: 6379
app:
  jwt:
    secret: ${JWT_SECRET:default_secret_key_cho_moi_truong_dev_chi_it_nhat_32_ky_tu}
    expiration: 86400000
    refresh-expiration: 604800000
springdoc:
  swagger-ui:
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  use-fqn: true